require "rake/testtask"

Rake::TestTask.new(:test) do |t|
  t.libs << "test" << "lib"
  t.test_files = FileList["test/test_*.rb"]
  t.warning = false
end

desc "Regenerate primitives.json from the canonical ToolSpec"
task :spec do
  sh "ruby bin/mud-manager --dump-spec > primitives.json"
end

task default: :test
